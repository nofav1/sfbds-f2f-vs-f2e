# Legacy pilots (gap F2E)

Frozen `pilot_corridor` / `pilot_open` / `pilot_maze` / `pilot_random` from before the pair-bound switch. Do not write new runs here. The runner refuses `output_dir` under `results/*/legacy/`.

Historical YAMLs: [`configs/pilot/retired/`](../../../configs/pilot/retired/). Pair-bound pilots: [`../pair-bound/`](../pair-bound/).
