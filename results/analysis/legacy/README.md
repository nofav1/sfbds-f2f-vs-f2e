# Legacy analysis snapshots

This directory is an **index of snapshots** of the **gap** F2E study (`results/study/legacy`). Do not dump `paired.csv` here. New pair-bound analyses go under [`../pair-bound/`](../pair-bound/).

Each dated folder has a generated `README.md`. Do not overwrite a previous slug.
